extends ../layout

block content

    .jumbotron.jumbotron-fluid
      .container
        h1 Pregled dostupnih poslova
        p
          | Trenutna potražnja za volonterima.
    .container
      .container
        if !user || !user.english_level
            table.table
                - each project, i in projects
                
                    thead.thead-light
                        tr
                        th(scope='col') Posao ID
                        th(scope='col') #{project._id}
                    tbody
                        tr
                            th(scope='row') Naziv organizacije
                            td #{project.creator_id.email}
                        tr
                            th(scope='row') Email organizacije
                            td #{project.creator_id.email}
                        tr
                            th(scope='row') Telefon organizacije
                            td #{project.creator_id}
                        tr
                            th(scope='row') Naziv posla
                            td #{project.title}
                        tr
                            th(scope='row') Opis posla
                            td #{project.description}
                        tr
                            th(scope='row') Lokacija
                            td #{project.location}
                        tr
                            th(scope='row') Početak
                            td #{project.start}
                        tr
                            th(scope='row') Kraj
                            td #{project.end}
                         
                         tr
                            th(scope='row') 
                            td 

        else
            table.table
                - each project, i in projects
                
                    thead.thead-light
                        tr
                        th(scope='col') Posao ID
                        th(scope='col') #{project._id}
                    tbody
                        tr
                            th(scope='row') Naziv organizacije
                            td #{project.creator_id.email}
                        tr
                            th(scope='row') Email organizacije
                            td #{project.creator_id.email}
                        tr
                            th(scope='row') Telefon organizacije
                            td #{project.creator_id}
                        tr
                            th(scope='row') Naziv posla
                            td #{project.title}
                        tr
                            th(scope='row') Opis posla
                            td #{project.description}
                        tr
                            th(scope='row') Lokacija
                            td #{project.location}
                        tr
                            th(scope='row') Početak
                            td #{project.start}
                        tr
                            th(scope='row') Kraj
                            td #{project.end}
                        
                        tr
                            th(scope='row') 
                            td 
                               a.btn.btn-secondary(href='#') Prijavi se
                         tr
                            th(scope='row') 
                            td 
        a(href='/') Nazad